# Glintide AI 开发规则

> 本文是后续 AI 参与 Glintide 主题开发时必须遵守的工程规则。
> 目标是让每次修改都具备清晰架构、明确边界、稳定行为和可持续维护能力。

## 1. 项目背景

Glintide 是一个 WordPress 主题项目。

当前项目包含两个不同用途的目录：

```text
项目根目录       当前实际运行的 Glintide 主题
reference/       参考代码、旧版实现和功能移植素材
```

`reference/` 不是当前主题的运行代码。除非任务明确要求参考或移植，否则不得直接修改、复制或依赖其中的文件。

当前主题的重要入口和配置约定：

```text
functions.php                  主题启动入口
style.css                      当前主题主样式
inc/                           当前后台和功能代码
inc/options/                   Codestar 和主题配置
inc/mod/                       主题功能模块和 REST API
inc/widgets/                   可插拔小工具
assets/                        当前主题前端资源
backup/                        旧版主题备份，仅用于恢复或参考
```

主题配置使用：

```php
get_option( 'glintide_options' );
glintide_get_option( $option, $default );
```

当前 REST 命名空间使用：

```text
/glintide/v1
```

## 2. 总体工程目标

所有架构、代码和结构决策必须优先满足以下目标：

1. 正确性优先于视觉效果和开发速度。
2. 清晰边界优先于快速堆叠代码。
3. 可维护性优先于短期少写文件。
4. 复用成熟能力，避免重复实现。
5. 优先使用 WordPress 原生生命周期和 API。
6. 修改范围必须与任务范围一致。
7. 前台重写不能破坏后台、设置、REST 和已有数据。
8. 任何新功能都要有明确的入口、数据来源、状态和失败处理。
9. 代码必须让下一位工程师可以快速定位和修改。
10. 不确定时先检查现有代码和运行链路，不得凭文件名猜测行为。

## 3. 工作前必须完成的分析

开始编码前，必须先完成以下检查：

### 3.1 确认当前真实代码

必须区分：

- 当前根目录文件
- `reference/` 参考文件
- `backup/` 备份文件
- 当前 WordPress 实际加载的文件

不得把 `reference/` 或 `backup/` 当成当前运行实现。

### 3.2 阅读相关调用链

修改某个功能前，至少确认：

```text
入口文件
→ 加载文件
→ 函数或类
→ 模板输出
→ CSS/JS 依赖
→ WordPress 钩子
→ 数据存储或 REST 接口
```

不能只改看到的模板文件，而忽略它依赖的函数、样式、脚本和配置。

### 3.3 检查工作区状态

如果可以使用版本控制，先检查工作区状态。必须保留用户已有修改，不得使用以下破坏性操作：

```text
git reset --hard
git checkout -- .
rm -rf 未确认目录
```

除非用户明确要求，不得删除或覆盖与当前任务无关的文件。

### 3.4 判断任务类型

先判断任务属于：

- 架构设计
- 新功能
- 前台页面重写
- 既有组件修改
- Bug 修复
- 性能优化
- 安全审查
- 代码重构
- 纯文档

不同任务使用不同范围。前台重写不等于重写后台，功能修复不等于顺便整理全项目。

## 4. 前后台边界

### 4.1 后台代码默认不可改行为

以下内容属于后台或核心功能层，除非用户明确要求，否则不得改变其行为：

- `inc/options/`
- Codestar Framework
- 主题设置存储键和字段 ID
- REST API 的已有接口契约
- 数据库字段、用户元数据和文章元数据
- 已有权限判断
- 已有小工具注册机制
- 已有后台菜单和设置页面
- 已有第三方服务配置

如果前台需要使用后台数据，应通过现有函数、设置读取函数或明确的接口获取，不得在模板中直接重复实现后台逻辑。

### 4.2 前台代码范围

前台重写可以涉及：

- `header.php`
- `footer.php`
- `index.php`
- `archive.php`
- `single.php`
- `page.php`
- `404.php`
- `comments.php`
- `tpl/`
- 前台专用 CSS
- 前台专用 JavaScript
- 前台模板所需的渲染辅助函数

新建前台文件时，应优先保持职责单一，不要把大量 HTML、数据库查询和业务规则全部写进一个模板。

### 4.3 保留文件

用户明确保留的文件必须优先保护，例如：

- 当前 `style.css`
- 当前封面图
- `assets/images/` 中的资源
- 后台 `inc/` 代码
- 用户已经修改但未提交的文件

除非用户明确要求，不得用 `backup/` 中的旧文件覆盖这些文件。

## 5. 目录和模块设计

### 5.1 推荐的前台结构

当前前台逐步重写时，推荐采用以下结构：

```text
.
├── functions.php
├── header.php
├── footer.php
├── index.php
├── archive.php
├── single.php
├── page.php
├── 404.php
├── comments.php
├── style.css
├── assets/
│   ├── css/
│   ├── js/
│   ├── images/
│   └── fonts/
├── tpl/
│   ├── content.php
│   ├── content-none.php
│   ├── content-card.php
│   └── parts/
└── inc/
    ├── options/
    ├── mod/
    ├── widgets/
    └── frontend/
```

如果项目规模较小，不必为了形式强行拆分目录；但模板、业务逻辑、资源和后台代码必须有清楚边界。

### 5.2 模块职责

每个模块必须有单一主要职责：

- 模板：组织页面结构和调用渲染函数
- 内容模板：输出单个内容对象
- 渲染函数：生成可复用 HTML 或数据结构
- 功能模块：处理业务、数据和 WordPress 钩子
- CSS：负责视觉表现和响应式布局
- JavaScript：负责交互、状态和浏览器行为
- 主题配置：定义后台字段和默认值

不要在模板中直接放：

- 大段 SQL
- 第三方 API 请求
- 复杂权限判断
- 重复的数据格式化逻辑
- 与页面无关的全局初始化

### 5.3 命名规范

当前主题的新代码统一使用 `glintide_` 前缀或 `Glintide_` 类名前缀：

```php
function glintide_render_card() {}
class Glintide_Navigation {}
```

CSS 类名统一使用：

```text
.glintide-组件名
.glintide-组件名__元素
.glintide-组件名--状态
```

JavaScript 全局变量应尽量避免。需要暴露的全局对象必须使用：

```text
window.Glintide
```

不得为新功能使用 `pix_`、`ppo_` 作为主要命名空间，除非是在兼容旧接口。

## 6. WordPress 开发规范

### 6.1 使用正确的钩子

应根据生命周期选择合适的钩子：

- `after_setup_theme`：主题支持、菜单注册
- `widgets_init`：小工具区域注册
- `wp_enqueue_scripts`：前台资源
- `admin_enqueue_scripts`：后台资源
- `rest_api_init`：REST 路由
- `wp_head`：必要的头部输出
- `wp_footer`：必要的底部输出

不得在模板每次渲染时重复注册钩子或重复加载文件。

### 6.2 资源加载

所有 CSS 和 JavaScript 必须通过 WordPress enqueue 系统加载：

```php
wp_enqueue_style();
wp_enqueue_script();
```

不要在模板中直接写：

```html
<script src="..."></script>
<link rel="stylesheet" href="...">
```

资源必须：

- 使用稳定的 handle
- 声明依赖关系
- 使用主题版本或文件修改时间作为版本号
- 仅在需要的页面加载
- 避免重复加载同一个库

### 6.3 模板安全输出

根据数据类型使用正确的转义函数：

```php
echo esc_html( $text );
echo esc_attr( $value );
echo esc_url( $url );
echo wp_kses_post( $html );
```

所有来自用户、设置、URL 参数、REST 请求和数据库的数据，都必须在输出边界进行验证或转义。

禁止直接输出未经处理的：

```php
echo $_GET['value'];
echo $user_input;
echo $url;
```

### 6.4 输入校验和权限

输入必须经过适合类型的处理：

```php
absint();
sanitize_text_field();
sanitize_key();
esc_url_raw();
wp_unslash();
```

写入操作必须同时考虑：

- 登录状态
- 用户能力
- Nonce
- 对象归属
- 数据类型
- 失败返回

不能因为接口暂时是内部使用，就省略权限校验。

### 6.5 REST API

新接口必须使用当前命名空间：

```text
/glintide/v1
```

每个接口必须明确：

- HTTP 方法
- 参数定义
- 参数校验
- `permission_callback`
- 成功返回结构
- 错误码和 HTTP 状态
- 是否允许公开访问
- 是否需要 Nonce

不得随意修改已有接口的字段名称和返回结构。需要升级时应保留兼容逻辑或明确记录迁移方案。

## 7. 前台架构设计规则

### 7.1 模板只负责组装

模板应该表达页面结构：

```php
get_header();
get_template_part( 'tpl/content', 'card' );
get_footer();
```

复杂逻辑应放到：

- `inc/frontend/`
- 已有功能模块
- 可复用的渲染函数
- 内容模板

### 7.2 组件必须有完整状态

前台组件不能只实现“正常显示”，至少要考虑任务相关状态：

- 正常
- 悬停
- 聚焦
- 激活
- 禁用
- 加载中
- 空数据
- 错误
- 成功反馈
- 移动端布局

### 7.3 导航和布局

导航组件必须明确区分：

- 桌面导航
- 平板导航
- 移动端导航
- 当前项状态
- 子菜单状态
- 键盘操作
- 抽屉或弹层关闭方式

布局不得依赖某个无关小工具是否启用。核心导航属于页面结构，不应因为侧栏为空而消失。

### 7.4 响应式规则

响应式必须从内容和操作关系出发，而不是只缩小尺寸。

至少检查：

- 320px 手机宽度
- 768px 平板宽度
- 1024px 笔记本宽度
- 1440px 桌面宽度
- 长标题
- 长菜单项
- 无图片状态
- 空内容状态
- 大量内容状态

不得让文字、按钮、图片或导航互相遮挡。

### 7.5 样式系统

优先复用已有 CSS 变量：

```css
--glintide-radius
--glintide-shadow-card
--glintide-sidebar-left-width
--glintide-center-width
--glintide-sidebar-right-width
```

新增颜色、字号、间距和圆角前，先检查是否已有对应变量或组件规则。不得在不同文件中随意创建同义变量。

## 8. JavaScript 规则

### 8.1 交互边界

JavaScript 只负责浏览器交互，不重复承担 PHP 的权限判断和核心业务规则。

前端可以：

- 控制菜单和抽屉
- 处理表单交互
- 请求 REST API
- 更新加载状态
- 展示成功和错误反馈

前端不能作为唯一的：

- 权限校验
- 价格校验
- 数据合法性校验
- 对象归属判断

### 8.2 事件处理

优先使用：

- 事件委托处理动态内容
- `addEventListener`
- 可取消或可重复初始化的初始化函数
- 明确的数据属性

避免：

- 重复绑定事件
- 依赖元素固定顺序
- 大量内联 `onclick`
- 修改全局原型
- 无限制轮询

### 8.3 可访问交互

交互控件必须使用正确语义：

- 跳转使用 `<a>`
- 动作用 `<button>`
- 弹窗有 `role="dialog"` 和 `aria-modal`
- 菜单按钮有 `aria-expanded`
- 隐藏内容同步 `aria-hidden`
- 图标有适当的 `aria-hidden` 或可读标签
- 键盘可以完成打开和关闭

## 9. 性能规则

每次前台开发都要注意：

- 图片使用合适尺寸和懒加载
- 首屏主要资源优先加载
- 不在每个循环中重复执行昂贵查询
- 避免无必要的第三方 CDN
- 避免重复加载 jQuery 或图标库
- REST 请求需要处理超时和错误
- 大量列表需要分页、限制或缓存
- 动画不能阻塞滚动和输入
- 不得为了视觉效果引入无法解释的大型依赖

如果使用外部 API，必须考虑：

- 连接失败
- 超时
- 返回格式变化
- 频繁刷新
- 服务不可用时的降级结果

## 10. 数据库和设置规则

修改设置字段、元数据或数据库表之前，必须确认：

1. 是否已有旧字段。
2. 是否有其他代码读取该字段。
3. 是否有默认值。
4. 是否需要兼容旧数据。
5. 是否需要迁移或回滚。
6. 是否会影响后台已有配置。

不允许为了方便直接更换已有 option key、meta key 或 REST 字段。

设置读取必须提供合理默认值：

```php
$value = glintide_get_option( 'some_option', $default );
```

## 11. 参考代码移植规则

从 `reference/` 移植功能时，必须经过以下步骤：

1. 确认该功能是否已经存在于根目录。
2. 找出参考实现依赖的函数、类、配置和资源。
3. 区分可复用的业务逻辑与旧版页面结构。
4. 改用 Glintide 命名空间。
5. 改用 `glintide_options` 配置前缀。
6. 改用 `glintide/v1` REST 命名空间。
7. 检查旧依赖是否在当前项目存在。
8. 补充输入校验、权限和错误处理。
9. 只移植任务需要的部分。
10. 记录移植来源和兼容差异。

不得直接复制整套参考主题的 `functions.php`、`inc/global.php`、模板或 CSS 覆盖当前主题。

## 12. 修改流程

每次实际编码按以下流程执行：

### 阶段一：理解

- 明确用户目标
- 确认影响页面
- 确认设备范围
- 区分前台和后台
- 阅读相关入口和调用链

### 阶段二：设计

- 选择最小合理改动范围
- 设计目录和模块职责
- 确认数据流
- 确认状态和错误处理
- 确认兼容和回滚方式

### 阶段三：实现

- 先实现数据和结构
- 再实现交互
- 最后调整样式
- 保留现有命名和变量体系
- 添加必要但简短的注释

### 阶段四：验证

- 检查模板是否完整
- 检查 PHP 语法
- 检查 JavaScript 语法
- 检查资源路径
- 检查关键页面
- 检查桌面和移动端
- 检查空数据和错误状态
- 检查是否影响后台

### 阶段五：交付

必须说明：

- 修改了哪些文件
- 每个文件负责什么
- 保留了哪些后台能力
- 使用了哪些静态数据或默认值
- 做了哪些验证
- 哪些验证无法执行以及原因
- 后续还需要什么配置或迁移

## 13. 代码质量标准

提交代码前必须自检：

### 架构

- 是否修改了正确目录
- 是否误用了 `reference/`
- 是否保持了前后台边界
- 是否避免了重复实现
- 是否有明确模块职责

### PHP

- 是否有主题前缀
- 是否避免未定义变量
- 是否正确转义输出
- 是否正确校验输入
- 是否检查函数或类存在
- 是否正确处理错误
- 是否避免在模板中堆积业务逻辑

### JavaScript

- 是否避免重复绑定
- 是否有加载、空数据和错误状态
- 是否支持键盘操作
- 是否正确处理异步失败
- 是否避免污染全局作用域

### CSS

- 是否复用已有变量
- 是否覆盖桌面、平板、手机
- 是否处理长文本和溢出
- 是否有聚焦和交互状态
- 是否避免无意义的绝对定位
- 是否避免影响后台页面

### WordPress

- 是否使用正确钩子
- 是否通过 enqueue 加载资源
- 是否保留 `wp_head()`、`wp_footer()`
- 是否保留 `wp_body_open()`
- 是否保留主题必需的 `style.css` 和 `index.php`
- 是否没有破坏已有 option、meta 和 REST 契约

## 14. 禁止事项

除非用户明确要求，不得：

- 修改 `reference/` 作为当前实现
- 删除 `backup/`
- 删除后台 `inc/`
- 重命名已有配置字段
- 改变已有 REST 接口返回结构
- 删除数据库字段或用户元数据
- 在模板中写未经转义的数据
- 把权限判断只放在 JavaScript
- 使用硬编码的外部域名替代主题 URL
- 为小改动引入大型依赖
- 用一份巨型文件承载整个前台
- 用复制粘贴制造多个几乎相同的模板
- 删除 `index.php` 或 `style.css`
- 未验证就声称页面已经可用

## 15. 最终验收标准

一个改动只有同时满足以下条件，才算完成：

- 当前主题可以被 WordPress 正确识别
- 前台模板结构完整
- 后台设置和功能代码没有被意外改变
- 代码职责清晰，后续可以继续扩展
- 关键输出经过正确转义
- 交互有正常、加载、空数据和错误状态
- 桌面端和移动端没有明显布局破坏
- 资源路径正确
- 没有依赖 `reference/` 才能运行
- 有实际验证记录
- 变更文件和剩余风险已经向用户说明

## 16. 默认决策原则

当需求没有明确实现细节时，按以下优先级决策：

```text
用户明确要求
> 当前项目已有约定
> WordPress 官方实践
> 最小改动和可回滚
> 性能与安全
> 视觉和代码偏好
```

如果存在两个都合理的方案，选择：

- 依赖更少
- 边界更清晰
- 更容易测试
- 更容易回滚
- 更符合当前主题已有结构
- 更不容易影响后台和已有数据

任何不确定但会影响数据、后台或大范围文件的操作，必须先停下来确认，不能擅自执行。
